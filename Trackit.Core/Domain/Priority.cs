using System;
using System.Collections.Generic;

namespace Trackit.Core.Domain;

// Declares an enumeration type named Priority.
// Enums are value types used when you have a known, limited list of options.
public enum Priority { Low = 0, Medium = 1, High = 2 }
